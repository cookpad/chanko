%h1 rendering view
= invoke(:sample_unit, :render)
- invoke(:sample_unit, :render2)
= invoke(:sample_unit, :render_partial_haml)
- invoke(:sample_unit, :render_with_block) do
  - @default = true
  render_with_block_default_line_haml
  finish

= invoke(:sample2_unit, :samename)
= invoke(:sample_unit, :samename)
