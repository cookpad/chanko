%h1 rendering view
= invoke(:sample_ext, :render)
- invoke(:sample_ext, :render2)
= invoke(:sample_ext, :render_partial_haml)
- invoke(:sample_ext, :render_with_block) do
  - @default = true
  render_with_block_default_line_haml
  finish

= invoke(:sample2_ext, :samename)
= invoke(:sample_ext, :samename)
